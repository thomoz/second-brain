# Optional FRED API key — get a free key at fred.stlouisfed.org
# Without this, yield curve and recession probability columns will be NULL.
FRED_API_KEY=

# Manual macro override (0-100). 50 = neutral.
# Lower in recession risk environments (e.g. 35), higher in favourable conditions (e.g. 70).
MACRO_SCORE=50
