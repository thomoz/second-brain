
## Memory Reflection (2026-06-11 → 2026-06-12)
**[memory_reflect]**
- Promoted to MEMORY.md: Billy Goat Karaoke shows at The Criterion Hotel — every Thursday at 8pm (new "Venues & Show Schedule" section)
- USER.md: no changes (no new profile patterns)

## 11:08
**[Session summary]**
- **Windows path bug in Python `-c` commands**: backslashes cause `unicodeescape` errors; fix with `$path -replace '\\\\', '/'` before passing to Python inline code
- **Config confirmed**: `DAILY_DIR = Memory\daily`; `now_local()` returns correct local time (11:07 SAST on 2026-06-12)
- **Scripts venv path**: `.\.venv\Scripts\python.exe` relative to `.claude\scripts\` directory works correctly
- Branch still `phase-2-pi-foundation` despite being on Phase 7 work

## 11:13
**[Session summary]**
- `SessionEnd` hook may not populate `transcript_path`; hooks now fall back to constructing path from `session_id`
- Fallback path pattern: `~/.claude/projects/<encoded>/<session_id>.jsonl`
- Windows path encoding rule: each special char (`:`, `\`, space) → one `-` (no collapsing)
- Fix applied to both `session-end-flush.py` and `pre-compact-flush.py`
- Debug file `.claude/data/hook_debug.json` added to log raw hook event fields for diagnosis
- Daily log appends to `Memory/daily/YYYY-MM-DD.md` under `## HH:MM` timestamp entries

## 12:11
**[Reflection]** Promoted items from 2026-06-11 to MEMORY.md/USER.md
